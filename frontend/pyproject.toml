[tool.poetry]
name = "frontend"
version = "0.1.0"
description = "Frontend Component"
authors = ["miketvo <miketvo@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
